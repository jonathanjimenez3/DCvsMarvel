const btnSpiderman = document.querySelector('#spiderman-btn');
const btnIronman = document.querySelector('#ironman-btn');
const btnCapitan = document.querySelector('#capitan-btn');
const btnThor = document.querySelector('#thor-btn');
const btnHulk = document.querySelector('#hulk-btn');
const btnWarMachine = document.querySelector('#warMachine-btn');
const btnBlackWidow = document.querySelector('#blackWidow-btn');
const btnVision = document.querySelector('#vison-btn');
const btnFalcon = document.querySelector('#falcon-btn');
const btnHawkeye = document.querySelector('#hawkeye-btn');
const btnScarlet = document.querySelector('#scarlet-btn');
const btnPanther = document.querySelector('#panther-btn');
const btnAnt = document.querySelector('#ant-btn');

const btnBatman = document.querySelector('#batman-btn');
const btnSuperman = document.querySelector('#superman-btn');
const btnWonder = document.querySelector('#wonder-btn');
const btnFlash = document.querySelector('#flash-btn');
const btnAquaman = document.querySelector('#aquaman-btn');
const btnGreen = document.querySelector('#green-btn');
const btnCat = document.querySelector('#cat-btn');
const btnBlackLight = document.querySelector('#black-Light-btn');
const btnDeathstroke = document.querySelector('#deathstroke-btn');
const btnSupergirl = document.querySelector('#supergirl-btn');
const btnCyborg = document.querySelector('#cyborg-btn');


btnBatman.addEventListener('mouseenter', () => insertHeroPowerStats(70))
btnThor.addEventListener('mouseenter', () => insertHeroPowerStats(659))
btnIronman.addEventListener('mouseenter', () => insertHeroPowerStats(346))
btnCapitan.addEventListener('mouseenter', () => insertHeroPowerStats(149))
btnHulk.addEventListener('mouseenter', () => insertHeroPowerStats(332))
btnWarMachine.addEventListener('mouseenter', () => insertHeroPowerStats(703))
btnBlackWidow.addEventListener('mouseenter', () => insertHeroPowerStats(107))
btnVision.addEventListener('mouseenter', () => insertHeroPowerStats(697))
btnFalcon.addEventListener('mouseenter', () => insertHeroPowerStats(251))
btnHawkeye.addEventListener('mouseenter', () => insertHeroPowerStats(313))
btnScarlet.addEventListener('mouseenter', () => insertHeroPowerStats(579))
btnPanther.addEventListener('mouseenter', () => insertHeroPowerStats(106))
btnAnt.addEventListener('mouseenter', () => insertHeroPowerStats(30))
btnSuperman.addEventListener('mouseenter', () => insertHeroPowerStats(644))
btnWonder.addEventListener('mouseenter', () => insertHeroPowerStats(720))
btnFlash.addEventListener('mouseenter', () => insertHeroPowerStats(263))
btnAquaman.addEventListener('mouseenter', () => insertHeroPowerStats(38))
btnGreen.addEventListener('mouseenter', () => insertHeroPowerStats(298))
btnCat.addEventListener('mouseenter', () => insertHeroPowerStats(165))
btnBlackLight.addEventListener('mouseenter', () => insertHeroPowerStats(103))
btnDeathstroke.addEventListener('mouseenter', () => insertHeroPowerStats(216))
btnSupergirl.addEventListener('mouseenter', () => insertHeroPowerStats(643))
btnCyborg.addEventListener('mouseenter', () => insertHeroPowerStats(194))
btnSpiderman.addEventListener('mouseenter', () => insertHeroPowerStats(620))
